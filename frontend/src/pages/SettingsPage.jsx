import Card from '../components/common/Card'; import Badge from '../components/common/Badge'; import SectionHeader from '../components/common/SectionHeader';
export default function SettingsPage({ health, config }) { return <><SectionHeader eyebrow="Privacy and local operation" title="Settings & Privacy">Safe public configuration for this local application.</SectionHeader><div className="two-column"><Card><h2>Local system status</h2><p>Application: <Badge tone="success">Local CandidateAI</Badge></p><p>Chat model: {config?.configured_chat_model || config?.configured_model || 'Not available'}</p><p>Job Match model: {config?.configured_job_match_model || 'Not available'}</p><p>Ollama: {health?.ollama?.reachable ? 'Connected locally' : 'Unavailable'}</p><p>Candidate profile: {health?.candidate_profile?.valid ? 'Available and valid' : 'Unavailable'}</p><p>Database: {health?.database?.available ? 'Available locally' : 'Unavailable'}</p></Card><Card><h2>Data handling</h2><p>Local processing only — no data leaves this Mac.</p><p>Conversation history is stored in the local application database and can be deleted from Conversations.</p><p>Saved analyses are stored separately in the local application database and can be deleted from Saved Analyses.</p><p>No cloud APIs, telemetry, analytics, remote fonts, or external assets are used.</p></Card></div></>; }
