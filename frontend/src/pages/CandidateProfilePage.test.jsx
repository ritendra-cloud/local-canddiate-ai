import { render, screen } from '@testing-library/react'; import CandidateProfilePage from './CandidateProfilePage';
test('renders approved profile sections without import metadata', () => { render(<CandidateProfilePage profile={{ candidate: { name: 'Candidate', professional_summary: 'Summary' }, skills: { programming: [{ name: 'Python' }] }, experience: [{ role: 'QA Lead', company: 'Example', responsibilities: ['Lead testing'] }], achievements: ['Achievement'], education: [{ degree: 'BE', institution: 'College' }], certifications: ['Certificate'], publications_and_patents: ['Patent'] }} />); expect(screen.getByText('Python')).toBeInTheDocument(); expect(screen.getByText('QA Lead')).toBeInTheDocument(); expect(screen.queryByText(/import_metadata/i)).not.toBeInTheDocument(); });
