import Card from '../common/Card'; import RequirementCard from './RequirementCard'; import MatchScore from './MatchScore';
const groups = [['Matched requirements', 'matched_requirements'], ['Partial matches', 'partial_matches'], ['Missing requirements', 'missing_requirements'], ['Unclear requirements', 'unclear_requirements']];
export default function MatchSummary({ analysis }) { return <div className="match-summary"><MatchScore analysis={analysis} /><Card><h2>Recruiter summary</h2><p>{analysis.executive_summary}</p><p className="muted">Created {new Date(analysis.created_at).toLocaleString()} · {analysis.model}</p></Card>{groups.map(([title, key]) => analysis[key]?.length > 0 && <section key={key}><h2>{title}</h2>{analysis[key].map((item, index) => <RequirementCard key={`${key}-${index}`} requirement={item} />)}</section>)}<div className="two-column"><Card><h2>Candidate strengths</h2><ul>{analysis.candidate_strengths?.map((item) => <li key={item}>{item}</li>)}</ul><h2>Interview focus</h2><ul>{analysis.interview_focus_areas?.map((item) => <li key={item}>{item}</li>)}</ul></Card><Card><h2>Interview questions</h2><ul>{analysis.interview_questions?.map((item) => <li key={item}>{item}</li>)}</ul><h2>Limitations</h2><ul>{analysis.limitations?.map((item) => <li key={item}>{item}</li>)}</ul><h2>Scoring details</h2><p>Total weight: {analysis.scoring_details?.total_weight}; achieved: {analysis.scoring_details?.achieved_weight}</p><p>{analysis.scoring_details?.recommendation_cap_applied ? 'Recommendation cap applied for missing must-have requirements.' : 'No recommendation cap was applied.'}</p></Card></div></div>; }
