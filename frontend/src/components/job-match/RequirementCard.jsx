import Badge from '../common/Badge'; import Card from '../common/Card';
const tone = { MATCH: 'success', PARTIAL: 'warning', MISSING: 'danger', UNCLEAR: 'neutral' };
export default function RequirementCard({ requirement }) { return <Card className={`requirement requirement-${requirement.match_status?.toLowerCase()}`}><div className="requirement-heading"><h3>{requirement.requirement}</h3><Badge tone={tone[requirement.match_status] || 'neutral'}>{requirement.match_status}</Badge></div><p><strong>{requirement.importance}</strong> · {requirement.category} · {requirement.confidence} confidence</p><p>{requirement.explanation}</p>{requirement.resolved_evidence?.length > 0 && <div><strong>Evidence from candidate profile</strong><ul>{requirement.resolved_evidence.map((item) => <li key={item.reference}><span>{item.label}:</span> {item.value}</li>)}</ul><details><summary>Technical evidence references</summary><ul>{requirement.resolved_evidence.map((item) => <li key={item.reference}>{item.reference}</li>)}</ul></details></div>}</Card>; }
