export default function LocalStatus({health,config}){let text='Checking local services…'; if(health){text=health.ollama.reachable?(health.ollama.model_available?'Ollama and model ready':'Configured model is missing'):'Ollama unavailable'} return <section className="status" aria-live="polite"><strong>{text}</strong><span>Local processing only — no data leaves this Mac</span><span>Model: {config?.configured_model||health?.ollama?.configured_model||'qwen2.5-coder:7b'}</span></section>}
