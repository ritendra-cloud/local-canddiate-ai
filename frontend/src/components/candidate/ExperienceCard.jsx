import { useState } from 'react'; import Card from '../common/Card'; import Button from '../common/Button';
export default function ExperienceCard({ item }) { const [open, setOpen] = useState(false); const responsibilities = item.responsibilities || []; return <Card className="experience"><div><h3>{item.role || 'Role not specified'}</h3><p className="muted">{item.company}{item.location ? ` · ${item.location}` : ''}</p><p>{item.duration}</p></div>{responsibilities.length > 0 && <><ul>{responsibilities.slice(0, open ? responsibilities.length : 2).map((entry, index) => <li key={`${index}-${entry}`}>{entry}</li>)}</ul>{responsibilities.length > 2 && <Button variant="secondary" onClick={() => setOpen(!open)} aria-expanded={open}>{open ? 'Show less' : 'Show all responsibilities'}</Button>}</>}</Card>; }
