import Badge from '../common/Badge';
export default function Header({ health, config, theme, setTheme, onMenu }) { const live = health?.ollama?.reachable && health?.ollama?.model_available; return <header className="app-header"><button className="menu-button" onClick={onMenu} aria-label="Open navigation">☰</button><div><strong>Local CandidateAI</strong><span>Private candidate intelligence, powered locally</span></div><div className="header-status"><Badge tone={live ? 'success' : 'warning'}>{live ? 'Ollama connected' : 'Ollama unavailable'}</Badge><Badge>{config?.configured_model || 'Local model'}</Badge><Badge>Local only</Badge><label className="theme-label">Theme<select aria-label="Theme" value={theme} onChange={(e) => setTheme(e.target.value)}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label></div></header>; }
